{
  "name": "phitness",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/phitness/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/phitness"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "phitness:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "phitness:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "phitness:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "phitness:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "phitness:build:development"
        },
        "production": {
          "buildTarget": "phitness:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["coverage/apps/phitness"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/phitness"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/phitness/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "serve-static": {
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "phitness:build"
      }
    }
  },
  "tags": []
}
